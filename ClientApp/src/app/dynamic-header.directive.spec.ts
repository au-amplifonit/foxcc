import { DynamicHeaderDirective } from './dynamic-header.directive';

describe('DynamicHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
