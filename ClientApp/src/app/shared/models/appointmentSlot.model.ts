export interface AppointmentSlot {
    startDate: string;
    endDate: string;
    duration: string;
    resourceId: string;
    resourceDescription: string;
    title: string;
    backgroundColor: string;
    textColor: string;
    borderColor: string;
}
